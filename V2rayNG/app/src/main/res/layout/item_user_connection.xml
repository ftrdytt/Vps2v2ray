<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layoutDirection="rtl"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground">

    <!-- حاوية الصورة (تدعم لون القصة النشطة) -->
    <FrameLayout
        android:id="@+id/layout_item_avatar_container"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <ImageView
            android:id="@+id/iv_item_pfp"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="3dp"
            android:scaleType="centerCrop" />

        <!-- أيقونة التفاعل (الريأكت) تظهر فوق صورة المشاهد مثل انستغرام -->
        <TextView
            android:id="@+id/tv_item_reaction"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="bottom|end"
            android:gravity="center"
            android:background="@drawable/bg_comment_bubble"
            android:textSize="14sp"
            android:elevation="3dp"
            android:visibility="gone"
            tools:text="❤️"
            tools:visibility="visible"/>
    </FrameLayout>

    <!-- الاسم والمعرف -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp">

        <TextView
            android:id="@+id/tv_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/tv_item_username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- زر المتابعة -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_item_follow"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="متابعة"
        android:textSize="12sp"
        android:textColor="#FFFFFF"
        app:backgroundTint="#2196F3"
        app:cornerRadius="8dp"
        app:elevation="0dp"
        android:insetTop="0dp"
        android:insetBottom="0dp" />

</LinearLayout>
